server:
  port: 8080
  servlet:
    context-path: /api/products/

spring:
  application:
    name: api-products
  datasource:
    url: jdbc:mysql://localhost:3306/msproducts
    username: root
    password: m@rgarida2018
  jpa:
    open-in-view: true

logging:
  level:
    web: debug

